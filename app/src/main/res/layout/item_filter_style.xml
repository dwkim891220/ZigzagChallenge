<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/tv_itemFilterStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/defaultSpacing"
        android:paddingTop="@dimen/defaultHalfSpacing"
        android:paddingEnd="@dimen/defaultSpacing"
        android:paddingBottom="@dimen/defaultHalfSpacing"
        android:background="@drawable/selector_filter_style"/>
</FrameLayout>